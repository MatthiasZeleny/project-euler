namespace Numbers.BasicMath;

public class TwoDigitFraction(TwoDigitNumber numerator, TwoDigitNumber denominator)
{
    public TwoDigitNumber Numerator { get; } = numerator;
    public TwoDigitNumber Denominator { get; } = denominator;

}